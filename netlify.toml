[build]
  command = "mkdir -p public/css && npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Serve admin SPA correctly
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
